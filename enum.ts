//enum stores constants; duplicate value is not allowed here...

//enum type:
// numeric,
// string,
// heretogenous.

//numeric enum...


enum RequestType {
    Getdata,
    SaveData,
    DeleteData,
}

console.log(RequestType);
